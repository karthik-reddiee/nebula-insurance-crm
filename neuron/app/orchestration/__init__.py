"""Neuron orchestration: Agent Cards, registries, versioned YAML plans, A2A tasks.

The A2A-aligned *internal* profile (ADR-027): private capability manifests
(Agent Cards), a plan/head/tool registry, versioned YAML orchestration plans
validated at startup, and a task manager mapping A2A tasks onto the operation
store. No public A2A endpoint, public Agent Card, or external federation is
exposed in F0038.
"""
