# F0038 — Neuron Day-at-a-Glance Shell — Status

**Overall Status:** Done — `feature` run `2026-07-01-90a75ace` closed out 2026-07-02 and archived. All 8 stories (S0001–S0008) code-complete end-to-end; **gates G0–G8 all PASS** (`validate-feature-evidence` exits 0 at every stage). Real evidence: engine 491 / neuron 116 / FE 17 tests green; coverage neuron 88% / FE 86.2% / engine Cobertura; SAST 0 findings, secrets 0 leaks, engine + neuron SCA clean (DAST waived); Neuron container health/readiness smoke PASS; code review APPROVED WITH RECOMMENDATIONS + security review PASS; KG reconciled (14 code-index bindings). Phase A approved (plan run `2026-06-30-dbc93ab5`); Phase B architecture approved (plan run `2026-06-30-d1dd91f7`). **Open follow-up (carried to PM closeout, [high]):** the F0038 implementation is not yet committed to git — the operator will commit all changes together before merge.
**Last Updated:** 2026-07-01

> Phase A (PM requirements) complete + approved (G3). Phase B (architecture)
> authored at plan run `2026-06-30-d1dd91f7`: ADR-027 ratified + ADR-028 (persistence
> schema, cross-store write consistency, outreach authorization), `neuron.*` data
> model, Neuron + engine API contracts, 8 JSON schemas, `renewal:draft_outreach`
> Casbin rule, BLUEPRINT §4.7, feature ERD/C4, and full KG ontology bindings
> (G4 ontology sync PASSED). **G5 operator approval granted 2026-06-30.** The Story
> Signoff Provenance rows remain placeholders until reviews run during the `feature`
> action (rows are append-only audit history).

## Story Checklist

| Story | Title | Status |
|-------|-------|--------|
| F0038-S0001 | Neuron service bootstrap (runtime + registries + versioned orchestration) | In Progress — code + contract/unit tests complete (pending review/signoff) |
| F0038-S0002 | Day-at-a-Glance shell + zone-dispatch + message/component envelope | In Progress — Neuron server side + React shell complete + tests; live Renewals content is S0003 (pending review/signoff) |
| F0038-S0003 | Live Renewals zone (needs-attention list + drill context) | Code-complete end-to-end — needs-attention list + companion-context drill (engine + Neuron + React), tests green (pending review/signoff) |
| F0038-S0004 | Stub zones — inert "not yet active" payload | Code-complete — stub heads return typed `inactive` (no engine read, no CTA), FE renders "not yet active"; delivered in S0002/S0003 + dedicated stub tests (pending review/signoff) |
| F0038-S0005 | Renewal outreach draft (generate-on-click, persist, edit in-chat) | Code-complete end-to-end — engine outreach-draft + Neuron `outreach_drafter` (engine-first + idempotent record, ADR-028 §2) + FE in-chat editable draft; tests green (pending review/signoff) |
| F0038-S0006 | Mock-send (commit `Identified → Outreach`, fake SMTP) | Code-complete end-to-end — engine mock-send (atomic transition + simulated event, NO SMTP) + Neuron `mock_send` (just-in-time If-Match, idempotent record) + FE `[Send (mock)]`; tests green (pending review/signoff) |
| F0038-S0007 | CRM-scope guard (out-of-scope → polite redirect) | Code-complete end-to-end — deterministic scope guard + intent classifier (behind the LLM-mocked seam) on `POST /v1/messages`; in-scope routes to the head, out-of-scope/injection → polite redirect, ambiguous → clarify, classifier-failure fail-safe; guard decision recorded in the operation store; FE composer active (pending review/signoff) |
| F0038-S0008 | Companion telemetry (baseline timestamps + minimal secondary metrics) | Code-complete end-to-end — engine `POST /internal/telemetry/neuron-companion` (batch, Serilog `Nebula.Neuron.Companion`, user_id-must-match-subject, closed-shape PII boundary) + Neuron fire-and-forget emit (needs-attention-surfaced/draft-ready primaries + DAU/actioned/draft-generated/mock-sent) at the glance + draft + mock-send flows (pending review/signoff) |

## Required Role Matrix

_Required signoff roles, set in planning (Phase B). The Story Signoff Provenance table below records the run-level role verdicts against each story._

| Role | Required | Why Required | Set By | Date |
|------|----------|--------------|--------|------|
| Quality Engineer | Yes | Acceptance-criteria + telemetry/workflow-transition validation. | Architect | 2026-07-01 |
| Code Reviewer | Yes | Independent review of zone-dispatch, draft write, and mock-send logic. | Architect | 2026-07-01 |
| Security Reviewer | Yes | Token forwarding (on-behalf-of), new Casbin `renewal:draft_outreach`, audit/provenance, prompt-injection surface, and no model-generated markup execution. | Architect | 2026-07-01 |
| AI Engineer | Yes | `neuron/` bootstrap, classifier, A2A-aligned internal delegation, versioned YAML orchestration, specialist head registry, message/component contract, prompt provenance, and scope guard. | Architect | 2026-07-01 |
| DevOps | Yes | First runnable Neuron service likely changes runtime/env/container/health-check contracts; confirm exact scope during plan. | Architect | 2026-07-01 |
| Architect | Yes | Persistence/envelope/head-contract/A2A-profile/token-forwarding/component-contract/orchestration ADRs require explicit approval. | Architect | 2026-07-01 |

## Story Signoff Provenance

> Rows record the run-level role verdict applied per story for run `2026-07-01-90a75ace`
> (reviews executed 2026-07-02; Architect provenance from the G0 assembly-plan validation).
> Code Reviewer per-story verdict is APPROVED; the feature-level `APPROVED WITH
> RECOMMENDATIONS` (uncommitted-source + cleanup follow-ups) is carried in
> `code-review-report.md` and dispositioned at G8 PM closeout.

| Story | Role | Reviewer | Verdict | Evidence | Date | Notes |
|-------|------|----------|---------|----------|------|-------|
| F0038-S0001 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0001 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0001 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0001 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0001 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0001 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0002 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0002 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0002 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0002 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0002 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0002 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0003 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0003 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0003 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0003 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0003 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0003 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0004 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0004 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0004 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0004 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0004 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0004 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0005 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0005 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0005 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0005 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0005 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0005 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0006 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0006 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0006 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0006 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0006 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0006 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0007 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0007 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0007 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0007 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0007 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0007 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |
| F0038-S0008 | Quality Engineer | Quality Engineer (run 2026-07-01-90a75ace) | PASS | `test-execution-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0008 | Code Reviewer | Code Reviewer (run 2026-07-01-90a75ace) | APPROVED | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0008 | Security Reviewer | Security Reviewer (run 2026-07-01-90a75ace) | PASS | `security-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0008 | AI Engineer | AI Engineer (run 2026-07-01-90a75ace) | PASS | `code-review-report.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0008 | DevOps | DevOps (run 2026-07-01-90a75ace) | PASS | `deployability-check.md` | 2026-07-02 | Run-level role verdict applies to this story. |
| F0038-S0008 | Architect | Architect (run 2026-07-01-90a75ace) | PASS | `g0-assembly-plan-validation.md` | 2026-07-01 | Run-level role verdict applies to this story. |

## Notes

- Source of truth for scope: [`intake-brief.md`](./intake-brief.md) (signed off 2026-06-29).
- This feature is **not blocked on F0021**: interim timeline-event home + mock-send now.
- **G1 product decision (2026-06-30):** the non-live zones (Tasks, Pipeline, Broker activity) are **shown as visible "not yet active" stubs**, not hidden until live — resolving the intake brief's lone open micro-decision (§5 "confirm vs hiding-until-live"). This advertises the multi-head pathway and sets up F0040's "flip a stub to live."
- **G1 decisions:** 8 fine-grained vertical slices (vs coarser bundling); personas reused from `nebula-personas.md` (no new persona files). Plan run `2026-06-30-dbc93ab5`, base evidence at `planning-mds/operations/evidence/runs/2026-06-30-dbc93ab5/`.
- KG: F0038 feature-mapping bindings authored by the architect in Phase B; `code-index.yaml` bindings to the as-built `neuron/` source are reconciled by the architect at feature gate G7.
- **S0001 implementation (2026-07-01, feature run `2026-07-01-90a75ace`):** Neuron service bootstrap landed under `neuron/app/**`, `neuron/crm_agents/cards/**`, `neuron/orchestration/plans/**`, `neuron/tests/**`. Delivered: stateless FastAPI runtime (`/health`, `/ready`; `/v1/*` return typed 501 until S0002+); Agent-Card + tool registries; versioned YAML plan loader/validator that fails fast on an unregistered head/tool/terminal-state or a missing `on_failure` (no silent fallthrough); Neuron-owned `neuron.*` operation store — in-memory impl behind the repository interface plus the durable 6-table schema migration (ADR-028 §1); engine client forwarding the user authentik token with typed upstream-unavailable/auth errors (no 500 leak); deterministic mocked model provider behind the router seam; A2A task manager writing threads/agent-runs/tool-calls/provenance with an idempotent cross-store engine reference (ADR-028 §2). **55 contract/unit tests pass** — `cd neuron && python3 -m unittest discover -s tests -t .`. Behavioral handlers (Renewals read S0003, stub zones S0004, outreach drafter S0005/S0006, scope guard S0007, telemetry S0008) are registered as placeholders and delivered in their slices. Story Signoff Provenance rows remain placeholders until reviews run.
- **S0002 Neuron server side (2026-07-01, run `2026-07-01-90a75ace`):** zone-dispatch glance assembly + versioned multi-part message envelope. `GET /v1/glance` enumerates the registered specialist heads and dispatches each into its own slot **in parallel with per-zone error isolation** (a failing head yields a typed `error` slot; others still render) — assembly, not composition. Renewals slot returns `empty` (live engine read is S0003); the three stub zones return the typed `inactive` payload (S0004, folded in as it is part of S0002's own acceptance criteria). Envelope + zone payloads validate against `neuron-message-envelope`/`neuron-zone-payload`; Neuron emits only registered component identifiers (server-side component registry — no model-generated markup). Owner-scoping via `subject_from_token` (JWT `sub`, unverified — engine authorizes). New: `app/envelope.py`, `app/auth.py`, `app/orchestration/zone_heads.py`, `app/orchestration/glance.py`; wired into `app/main.py` + `app/bootstrap.py`. **69 contract/unit tests pass** (14 new). Remaining for S0002: the React Day-at-a-Glance shell + component/action registry renderer (S0002 FE side).
- **S0002 React shell (2026-07-01, run `2026-07-01-90a75ace`):** the companion panel (`experience/src/features/neuron/`) now hosts the **Day-at-a-Glance shell**. `useGlance` (TanStack Query) fetches `/neuron/v1/glance` via the shared token-forwarding `api` client; the shell renders one slot per zone (Renewals prominent/first, three stub cards) with per-zone status states (content/empty/inactive/error) and loading/error/auth-required states. The **component/action registry** (`registry/`, AJV-validated) renders only registered component ids from validated props — an unknown id or invalid props renders a safe, non-executable fallback (intake L1: no model-generated markup). `NeuronPanel` keeps its `RightChatPanel` props/chrome; the free-text composer is inert until F0038-S0007. **10 FE tests pass** (`npx vitest run src/features/neuron`), `tsc -b` exit 0, eslint 0 errors. Live Renewals list content arrives with the engine read in F0038-S0003.
- **S0003 engine `GET /renewals/needs-attention` (2026-07-01, run `2026-07-01-90a75ace`):** the .NET engine now serves the Neuron Renewals zone. New `RenewalNeedsAttentionItemDto` (snake_case per `renewal-needs-attention-item.schema.json`), wrapped `{data:[...]}`; `RenewalService.GetNeedsAttentionAsync`; `RenewalRepository.ListNeedsAttentionAsync` (reuses the existing row-level auth scope; Identified/Outreach expiring < today+withinDays incl. overdue, soonest-first; broker-contact derived from outreach timeline events); the `GET /renewals/needs-attention` endpoint (Casbin `renewal:read`, `withinDays` 1..365). **2 new xUnit tests pass; full suite 462 passed / 0 failed / 1 skipped.** **Decisions to flag for the architect:** (1) the engine returns **snake_case** for this companion endpoint (conforms to the referenced schema + nebula-api.yaml), which deviates from the engine's usual camelCase — the Neuron head will map to the camelCase component props at the boundary; (2) `no_contact_flag` sources 'last broker contact' from `RenewalOutreachDrafted`/`RenewalOutreachMockSent` timeline events (populated by S0005/S0006) — null → flagged until then; (3) `can_draft_outreach` uses the `renewal:draft_outreach` Casbin check (added in S0005) — false until then, so the Draft CTA lights up automatically. Next: Neuron `RenewalsZoneHead` calls this endpoint as the user and returns the `content` zone payload; then the `companion-context` drill endpoint.
- **S0003 Neuron `RenewalsZoneHead` wiring (2026-07-01, run `2026-07-01-90a75ace`):** the live Renewals head now calls `engine.renewals.needs_attention` **as the user** (forwarded token) and maps the engine's snake_case items into the `renewals.needs_attention_list` component's camelCase props (`renewalId`/`accountName`/`expiresInDays`(from `days_to_expiry`)/`workflowState`/`noBrokerContact30d`(from `no_contact_flag`)/`brokerName`/`canDraftOutreach`). Returns `content` when renewals need attention, `empty` when none; an engine failure propagates and the orchestrator isolates it to the slot. Records the engine call in the A2A trace (`neuron.tool_calls`). `HeadContext` extended with `tools`/`task_manager`/`run`; glance assembler passes them through. **74 neuron tests pass** (5 new); end-to-end smoke confirmed the Renewals slot renders live-shaped data. This closes the needs-attention half of S0003; the `companion-context` drill endpoint + drill view remain.
- **S0003 companion-context drill (2026-07-01, run `2026-07-01-90a75ace`):** finishes S0003. **Engine:** `GET /renewals/{id}/companion-context` (camelCase per nebula-api.yaml — account/broker/workflow/expiry/canDraftOutreach + recent timeline; Casbin `renewal:read`, row-scoped, 404 when not visible or not Identified/Outreach) + `RenewalCompanionContextDto` + `GetCompanionContextAsync` + 3 xUnit tests. **Neuron:** `POST /v1/actions` now implemented — an `ActionDispatcher` handles `drill_renewal` (calls the engine companion-context tool as the user, returns an envelope with a `renewals.companion_context` app-part, records the tool call); `draft_outreach`/`mock_send`/`scope_redirect_ack` return a typed 501 until their slices; 6 new tests. **Frontend:** an `ActionContext` + `useCompanionAction` mutation; the Renewals rows get a **View** button that dispatches `drill_renewal`; the returned envelope is appended to the thread and rendered via the new registered `renewals.companion_context` component; 2 new tests (drill interaction + registry). **Verification:** .NET **465 passed / 0 failed / 1 skipped**; neuron **80**; FE **12** + `tsc -b` clean + eslint 0 errors. EF InMemory note recurred (required `AssignedToUser` Include drops rows on materialize without a seeded UserProfile — the companion-context test seeds it).
- **S0004 stub zones — closed out (2026-07-01, run `2026-07-01-90a75ace`):** the inert stub behavior was delivered by S0002/S0003 and is now explicitly verified. AC mapping: stub heads (`crm.tasks.head`/`crm.pipeline.head`/`crm.broker_activity.head`, `active:false`, `auth_mode:none`) return a typed `inactive` zone payload via `StubZoneHead` (`neuron/app/orchestration/zone_heads.py`) — no component/props/CTA, no engine read; the glance orchestrator dispatches all four zones and isolates each slot; the FE `ZoneSlot` renders the 'not yet active' placeholder. Added `neuron/tests/test_stub_zones.py` (3 tests) proving the typed `inactive` payload **and no engine call** (the S0004 DoD test requirement), alongside the existing glance + FE assertions (3 'not yet active' cards). Adding/removing a stub is a registry (card) change only. **83 neuron tests green.**
- **S0005/S0006 engine + security layer (2026-07-01, run `2026-07-01-90a75ace`):** the security-sensitive core. **Already in place from Phase B (confirmed):** the `renewal:draft_outreach` Casbin action (policy.csv Underwriter + Admin) and `WorkflowStateMachine.ValidateOutreachMockSendTransition` (Identified→Outreach, Underwriter/Admin-only, path-scoped per ADR-028 §3). **Added this slice:** `POST /renewals/{id}/outreach-draft` — persists an **InternalOnly** `RenewalOutreachDrafted` timeline event with provenance (model/promptId/version/contentHash/agentRunId) + the draft body; **no transition**; content-guard rejects premium/quote/coverage-terms/binding/currency (422). `POST /renewals/{id}/outreach-mock-send` — **atomic** `Identified→Outreach` `WorkflowTransition` + `RenewalOutreachMockSent` 'sent (simulated)' event, **no SMTP path invoked**, If-Match rowVersion (412), 409 when not Identified. Both Casbin-gated on `renewal:draft_outreach`. New: `RenewalOutreach*Dto` (snake_case requests / camelCase responses), `OutreachContentGuard`, `PersistOutreachDraftAsync` + `OutreachMockSendAsync`, 2 endpoints. **11 new xUnit tests** (4 state-machine exception incl. Distribution-denied; 7 draft/mock-send: InternalOnly persist + no transition, content rejection, invalid-state, atomic transition+event, non-Underwriter denied, invalid-transition, rowVersion 412). **Full suite 476 passed / 0 failed / 1 skipped.** Remaining: the Neuron `outreach_drafter` goal agent (wire `draft_outreach`/`mock_send` into `ActionDispatcher`; engine-first + idempotent Neuron record, ADR-028 §2) and the FE in-chat editable draft + `[Send (mock)]`.
- **S0005/S0006 Neuron drafter + FE (2026-07-01, run `2026-07-01-90a75ace`):** finishes both outreach stories end-to-end. **Neuron** (`app/actions.py` `ActionDispatcher` + `app/outreach_drafter.py`): `draft_outreach` generates a content-safe draft (LLM mocked; Neuron-side content guard), persists it **engine-first** via the outreach-draft tool, then writes an **idempotent** Neuron `agent_run` + provenance referencing the returned engine timeline-event id (ADR-028 §2), and returns an `outreach.draft_editor` envelope; `mock_send` guards the edited body, fetches the renewal's rowVersion **just-in-time** for the engine's If-Match, calls the mock-send tool, and records the Neuron run referencing the transition id (engine_client/tool gained `headers` support for If-Match). **Frontend** (`components/OutreachDraftEditor.tsx`): the registered `outreach.draft_editor` — editable draft with 'AI-generated draft' + InternalOnly labels, `[Edit]`, `[Send (mock)]` dispatching `mock_send` with the edited body; the Renewals row 'Draft outreach' button now dispatches `draft_outreach`. **Verification:** neuron **85 tests**, FE **14 tests** (incl. a full draft→edit→send flow), `tsc -b` clean, eslint 0 errors; an end-to-end smoke confirmed engine-first + idempotent cross-store refs [timeline_event, workflow_transition] and the just-in-time If-Match.
- **S0007 CRM-scope guard — DONE end-to-end (2026-07-01, run `2026-07-01-90a75ace`), pending review/signoff.** **Neuron:** new `app/scope_guard.py` — a deterministic `classify_intent` (LLM mocked; sits behind the same seam a real LLM classifier would, so swapping it in later is a registration change) with a security-first priority (injection / scope-escape markers checked BEFORE any CRM keyword, so a message can't smuggle itself in-scope by appending a CRM word to a break-character instruction), plus `evaluate_scope` → `GuardDecision` (allow+route / redirect / clarify) that **fails safe to redirect on any classifier error** (never an unbounded general answer). Bound both cards to behavioral handlers (`ScopeGuardHandler`/`IntentClassifierHandler`, replacing the `BootstrapHandler` placeholders in `bootstrap.py`). New `app/messages.py` `MessageDispatcher` implements `POST /v1/messages` (was typed 501): persists the user's turn in their private thread, runs the guard **before** any handler, routes in-scope renewals to the live `RenewalsZoneHead` (engine call as the user), turns stub-zone intents into a "not yet active" reply, and returns a polite CRM-redirect text envelope for out-of-scope/injection (same copy for both; injection recorded distinctly). The guard decision is recorded in the operation store (a `scope_guard` `agent_run` + a digest-only `scope_guard.classify` `tool_call` carrying the bounded intent label — never the user's raw text). Resolved the reserved `scope_redirect_ack` action (ActionDispatcher) as a benign no-op ack (no engine call / no state change) so no stale 501 points at a now-complete story. **Frontend:** new `hooks/useSendMessage.ts` (`POST /neuron/v1/messages`) + `components/Composer.tsx` (Enter-to-send, Shift+Enter newline); `DayAtAGlance.tsx` now hosts the **active composer** (was inert) — echoes the user turn, appends the scope-guarded reply, renders via the registry (redirect is plain text, never a model-generated component); `NeuronPanel.tsx` composer placeholder removed. **Verification:** neuron **106 tests** (+21; `python3 -m unittest discover -s tests -t .`), FE **17 tests** (+3; `npx vitest run src/features/neuron`), `tsc -b` exit 0, eslint 0 errors. The guard grants NO authorization — in-scope reads are still authorized by the engine as the user.
- **S0008 companion telemetry — DONE end-to-end (2026-07-01, run `2026-07-01-90a75ace`), pending review/signoff.** **Engine:** `POST /internal/telemetry/neuron-companion` (mirrors the F0035 SessionContinuity ingest, ADR-024) — new `NeuronCompanionTelemetryEndpoints` + `NeuronCompanionTelemetryService` (validates the 6-event registry, `event_version>=1`, and the schema `allOf` conditionals: needs-attention-surfaced/draft-ready require renewal_id+thread_id, companion-daily-active requires persona∈{underwriter,distribution}; batch 1..50) + `NeuronCompanionTelemetryModels` (closed DTO → structural PII boundary: no free payload, so a draft body/prompt/credential has nowhere to bind) + `ProblemDetailsHelper.NeuronCompanionTelemetryValidationError`; wired in `Program.cs`; accepted events → Serilog `Nebula.Neuron.Companion`; **user_id must match the authenticated subject** (mismatch → 403, mirroring F0035; treated as a string, not a Guid, to avoid the F0035 drop bug). Returns 202. **Neuron:** new `app/telemetry.py` `CompanionTelemetry` (fire-and-forget over the registered `engine.telemetry.ingest` tool — swallows+logs any failure so the flow never breaks; emits AFTER the authoritative write) + `build_event`; `auth.persona_from_token` (best-effort, telemetry-only, non-authoritative; defaults underwriter). Emit sites: **glance** → companion-daily-active + needs-attention-surfaced (primary start) per surfaced renewal; **draft** → draft-ready (primary end) + draft-generated + attention-renewal-actioned; **mock-send** → mock-sent. **Verification:** engine API build clean, **10 engine unit tests** (`NeuronCompanionTelemetryServiceTests`, no-Docker) pass + **5 integration tests** written (`NeuronCompanionTelemetryEndpointTests`, run at the in-container G2/QE slice — Docker unavailable in this bg session); neuron **116 tests** (+10; incl. correlation of the primary renewal_id/thread_id + a fire-and-forget-failure test). **ALL 8 STORIES CODE-COMPLETE — next is gates G2–G8.**
