# Coverage Report — F0028

## Summary

Focused coverage was collected through the backend test run and frontend route smoke tests. This closeout records targeted feature confidence rather than full suite coverage.

## Backend Coverage

- Focused backend test result: 111 passing tests for `CarrierMarketEndpointTests` and `CasbinAuthorizationServiceTests`.
- Coverage attachment generated by the backend test runner under `engine/tests/Nebula.Tests/TestResults/**/coverage.cobertura.xml`.
- Covered behavior includes carrier market profile lifecycle, child collection persistence, authorization policy availability, and unauthenticated endpoint protection.

## Frontend Coverage

- `App.test.tsx` route smoke tests passed after adding the `/carrier-markets` route mock.
- `npm run build` validates TypeScript and Vite production bundling for the new feature page and hooks.

## Harness Coverage

- `planning-mds/knowledge-graph/coverage-report.yaml` was refreshed.
- KG validation and drift checks passed with documented pre-existing warnings.

## Coverage Waiver

Full regression and numeric coverage threshold enforcement were not run in this local feature action. The waiver owner is Quality Engineer and the waiver was approved_on 2026-07-02. The waiver is accepted in `evidence-manifest.json` because focused F0028 behavior, authorization, build, and runtime health checks passed.
