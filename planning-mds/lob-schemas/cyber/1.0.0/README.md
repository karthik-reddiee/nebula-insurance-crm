# Cyber 1.0.0 LOB Schema Bundle

Canonical bundle for F0034. The runtime seed in migration `20260507030000_F0034_ProductSchemaRegistryAndLobAttributes` mirrors these data, UI, rule, and projection artifacts.

The backend validator enforces the required field set, revenue band enum, high-record MFA rule, and minimum retention rule. Frontend rendering uses the same envelope keys and error paths.
